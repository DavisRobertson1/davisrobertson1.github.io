# assets/

Drop static files here.

- `favicon.svg` — site icon (already present).
- **Resume PDF** — place the file here (suggested name `davis-robertson-resume.pdf`)
  and update the download link in `index.html` (the `#resume` section) to point at it.
- Any images referenced by the site.
